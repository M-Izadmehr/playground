branches:
  only:
    - master
    - /^\d+\.(\d+|x)(\.x)?$/

jobs:
  include:
    - stage: release
      node_js: lts/*
      before_install:
        - .travis/chmod +x build.sh
      install:
        - travis_retry npm install
      script:
        - .travis/push.sh
